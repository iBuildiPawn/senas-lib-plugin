# Onboarding

First-day-as-a-dev checklist. Keep this page short — a new engineer should be able to scan it in five minutes.

## Getting the repo running locally
1. <first step — probably `cp .env.example .env`>
2. <second step — probably `docker compose up` or `<pkg> install`>
3. <third step — running migrations / seeding>
4. <how to know it worked — e.g. "visit localhost:8080/health, expect 200">

## The five files every contributor should read
1. `<path>` — <why>
2. `<path>` — <why>
3. `<path>` — <why>
4. `<path>` — <why>
5. `<path>` — <why>

## Conventions specific to this repo
<Anything that would trip up a developer experienced with the stack but new to this codebase. Naming patterns, layer boundaries, testing conventions, commit-message style.>

## Common gotchas
- <Gotcha 1, cited>
- <Gotcha 2, cited>

## Where to ask
<If the team has a Slack channel, ticketing system, owner list, etc., pointer goes here. If you do not know, say "unknown — ask the team".>
