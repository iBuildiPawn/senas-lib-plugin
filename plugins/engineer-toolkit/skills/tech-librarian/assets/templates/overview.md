# Overview

## What this repo is
<One paragraph. Plain language. What does this codebase do at a technical level — what kind of system is it (web app, CLI, service, library), what does it produce or expose, and what's the one-sentence summary?>

## Shape
- **Layout:** <single app | monorepo of services | library | other>
- **Primary language(s):** <e.g. PHP 8.3, TypeScript, Python 3.12>
- **Primary framework(s):** <e.g. Laravel 11, Next.js 15>
- **Data stores:** <e.g. MySQL, Redis, S3>
- **Async / messaging:** <e.g. Kafka, SQS, none>
- **Deploy target:** <e.g. k8s on GKE, Fly.io, Vercel>

## At-a-glance units
<If monorepo: a bullet list of top-level units with a one-line purpose each. If single app: list of major modules / bounded contexts.>

- **`<unit-name>`** — <one-line purpose>

## Activity signal
<A quick, human read on where the team is spending time. E.g. "Heavy recent activity in Order-Server + Client-Server around promotions; Admin-Server mostly maintenance." Base this on `git log --since=...` or `git shortlog -sn`.>

## The "what to read first" trail
<3-5 files that a first-time reader should open in order. Each one with a one-line reason.>

1. `<path>` — <why read this first>
2. `<path>` — <why>
3. ...
